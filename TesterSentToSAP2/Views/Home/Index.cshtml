@{ ViewBag.Title = "Chat"; }
<h2>Chat</h2>
<div class="container">

</div>
@section scripts {
    <!--Script references. -->
    <!--The jQuery library is required and is referenced by default in _Layout.cshtml. -->
    <!--Reference the SignalR library. -->
    <script src="~/Scripts/jquery.signalR-2.4.3.js"></script>
    <!--Reference the autogenerated SignalR hub script. -->
    <script src="~/signalr/hubs"></script>
    <!--SignalR script to update the chat page and send messages.-->
    <script>
        $(function () {
            // Reference the auto-generated proxy for the hub.
            var chat = $.connection.signalRHub;
            // Create a function that the hub can call back to display messages.

            // yg harus dikirim : idhistory, status
            // Nilai Presentase : TotalCompletedData / TotalData * 100 (hanya untuk status on progress)
            chat.client.TestSend = function (name, message) {
                // Add the message to the page.
                $('.container').append('<strong>' + name
                    + '</strong>: ' + message + '<br/>');
            };

            $.connection.hub.start().done();
        });
        // This optional function html-encodes messages for display in the page.
        function htmlEncode(value) {
            var encodedValue = $('<div />').text(value).html();
            return encodedValue;
        }
    </script>
}